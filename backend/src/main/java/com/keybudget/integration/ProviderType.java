package com.keybudget.integration;

/** Identifies the external financial service provider for an integration. */
public enum ProviderType {
    COINBASE,
    BITCOIN_WALLET,
    M1_FINANCE,
    MARCUS
}
